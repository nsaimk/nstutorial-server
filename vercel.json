{
    "version": 2,
    "builds": [
        {
            "src": "cmd/api/main.go",
            "use": "@vercel/go",
            "config": {
                "GO_BUILD_FLAGS": "-ldflags '-s -w'"
            }
        }
    ],
    "rewrites": [
        {
            "source": "^/",
            "destination": "cmd/api/main.go"
        }
    ]
}